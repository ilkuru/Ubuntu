\ - слова Заканчивающиеся на text
- начало строки
$ - конец строки
[a-z] - диапазон от a - до z
[ t] - не буква t
\[ - воспринять символ [ буквально
. - любой символ
a|z - a или z

grep - утилита поиска по выражению (по дефу чуствит. к регистру)
egrep - расширенный grep
fgrep - быстрый grep
rgrep - рекурсивный
sed - потоковый текстовый редактор

cat file_name | grep 'text' #

#grep принимает некоторые регулярные выражения
grep 'что_ищем' file_name
grep -n 'что_ищем' file_name # c отображением N строки
grep -i 'что_ищем' file_name # игнорирует чуствит. к регистру
grep  ple file_name # ищет строки в начале которых 'ple'
grep ple$ file_name # ищет в конце строки
grep .o file_name # ищет сочетание '.'- любой символ посоле которонр идет буква 'o'

#egrep расширенный grep принимает все регулярные выражения
egrep ' (b|d)' file_name # искать с начала строки то что начинается на 'b' or 'd'
egrep ' [a-k]' file_name # искать с начала строки то что начинается c 'a' по 'k'
egrep ' ([a-k]|[A-K])' file_name # c 'a' по 'k', '|' - или c 'A' по 'K'

#fgrep не принимает регулярные выражения
fgrep c$ file_name # найдет не как 'c' в конце строки, а как 'c$'

#rgrep поиск по файлам и папкам

#sed потоковый редактор, меняет только вывод

sed -e 's/oo/aa/' file_name # заменяет в выводе все 'oo' на 'aa'
sed -e 's/oo/aa/' file_name > new_file_name # заменяет в выводе все 'oo' на 'aa' и сохраняет в новый файл
sed -re 's/ (B|b)/C/' file_name # '-r' подключить регулярные выраж.' ' с начала строки B или b заменить на 'C'