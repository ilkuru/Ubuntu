при входе usr в систему
etc/profile - файл с глобальными настройками оболочки
  запускает, левая если юзер входит, если уже вошел и запускает bash, запуститься правая
        |                  |
/etc/profile.d/*.sh     /etc/bash.bashrc (глобальный профиль пользователя, псевдонимы и функции)
     |                  /etc/bashrc(глоб. настр. для всех usr)
     |
~/.bash_profile
~/.bash_login
~/.profile ->  ~/.bashrc(наст. конкретного usr)

.profile - файл пользователя.
.bashrc - файл с настройками внешнего вида оснастки

при выходе пользователя из системы отрабатывает .bash_logout

/etc/skel # папка с профелем по умолчанию для каждого нового юзера.

#псевдонимы(alias) - вызов каманды с заданными ключами.


#псевдонимы и Функции добавляем в ~/.bashrc
alias ls='ls --color=auto'
alias la='ls -A'

#Функции - текст для вызова скрипта из нескольких команд.
function Hello(){
echo 'Hello, dear, I am awere for:';
uptime -p;
date;
}
# передача аргументорв в функцию
аргументы передаються через $1 - первый аргумент $2 второй и т.д $0-передает имя функции
для примера:
grep user_name /var/log/auth.log | tail -n 2 > user_name.txt

function showlog() {
date > $1.txt;
grep $1 /var/log/auth.log tail -n $2 >> $1.txt;
}
	        1$    $2
	        |	  |
showlog user_name 3

# переменные и настройки можно указать в настройках usr, будут загружаться с входом в систему:

переменные - работают в пределах текущей оболочки в 1 косноли
переменные среды - работают так же во всех дочерних процессах, если запустить из конколи еще консоль;
при закпытии консоли все переменные не сохраняються, для сохранения нужно добавить их в ~/.profile

export ABC - экспорт переменной в среду окружения

команды:
set - вывод всех переменных и функций
ABC = 123 - задаем переменную
unset ABC - удалить переменную из окружения
export ABC - экспорт переменной в среду окружения
env - выводит переменные среды

PATH = $PATH:. - добавляет к переменной PATH текущий путь PATH через ':' c
помощью '.' добавляет путь текущей папки для запуска исполняемых сприптов с
директории в которой находится польз.

echo $ABC - вывод переменной ABC

